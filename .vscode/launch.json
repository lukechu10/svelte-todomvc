{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "version": "beta",
            "request": "launch",
            "name": "Launch Edge against localhost",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}"
        }
    ],
    "compounds": []
}